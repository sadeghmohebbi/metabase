x-common-variables: &common-variables
  MB_EDITION: ee
  VERSION: v1.0.0.0

services:
  metabase-ee:
    build:
      context: .
      dockerfile: Dockerfile
      args: *common-variables
    environment: *common-variables
    image: portus.taaghche.com/taaghche/metabase-ee:latest
